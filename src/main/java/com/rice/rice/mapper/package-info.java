/**
 * 数据访问层（Mapper）：基于 Spring Data JPA，接口继承 {@link org.springframework.data.jpa.repository.JpaRepository}。
 * <p>
 * 命名说明：与 MyBatis 的 Mapper 概念对应，便于按 Controller → Service → Mapper 三层理解项目结构。
 */
package com.rice.rice.mapper;
